server:
  port: 0
spring:
  cloud:
    config:
      enabled: true
      discovery:
        service-id: discovery
      fail-fast: true
  application:
    name: order-service
  profiles:
    active: local

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8888/eureka/

